#include <iostream>

#ifndef ONLINE_JUDGE
#define DEBUG(X) std::cout<<X<<std::endl;
#else
#define DEBUG(X) ;
#endif

using namespace std;

int main() {
    int a;
    cin >> a;
    DEBUG(a)
    return 0;
}